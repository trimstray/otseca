###############################################################################
################################ DEBIAN STACK #################################
###############################################################################

DEBIAN_STACK=(\

  # Check umask.
  "_grep umask /etc/sysconfig/init" \
  # Check syslog for accept remote.
  "_grep SYSLOGD_OPTIONS /etc/sysconfig/syslog" \

  ### SERVICES ###

  ### PACKAGES ###
  "dpkg -l | grep setroubleshoot" \
  "dpkg -l | grep prelink" \
  "dpkg -l | grep mcstrans" \
  "dpkg -l | grep libselinux" \
  "dpkg -l | grep xorg-x11*" \
  "dpkg -l | grep iptables" \

)
